{
  "name": "localify-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c cyan,magenta \"npm --prefix server run start:dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "dev:server": "npm --prefix server run start:dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
